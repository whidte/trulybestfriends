# Project Rules

- This project has two branches: `master` (Forge 1.20.1, working tree at `D:\trulybestfriends-master`) and `1.21.1` (NeoForge 1.21.1, working tree at `D:\trulybestfriends`).
- EVERY code change must be applied to BOTH branches and each branch must be compile-verified (`gradlew compileJava`).
